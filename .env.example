# Port the server will run on
PORT=4000

# MongoDB connection string (replace with your actual URL in .env)
MONGODB_URL=mongodb://localhost:27017/your-db-name

# JWT secret used to sign tokens (set a secure value in .env)
JWT_SECRET=your_jwt_secret_here
